import { Button } from "./ui/button";

const MainNav = () => {
  return (
    <Button
      variant="ghost"
      className="font-bold hover:text-orange-500 hover:bg-orange-50 outline outline-orange-500"
    >
      Log In
    </Button>
  );
};

export default MainNav;
